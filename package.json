{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap -- --hoist",
    "dev": "lerna run --scope play dev"
  },
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
